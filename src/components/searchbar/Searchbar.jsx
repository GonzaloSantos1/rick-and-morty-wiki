import React from 'react';
import './_searchbar.scss';

const Searchbar = () => {
  return <input className='searchbar' type='text' placeholder='Search by character' />;
};

export default Searchbar;
